[build]
  base = "frontend"
  command = "(cd ../studio && npm ci) && npm ci && npm run build"
  publish = "dist"

[functions]
  directory = ".netlify/functions-internal"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000