[build]
  base = "frontend"
  command = "npm ci && (cd ../studio && npm ci) && npm run build"
  publish = ".output/public"

[functions]
  directory = ".netlify/functions-internal"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000